type Language = {
  value: string;
  label: string;
};

type HomePageBanner = {
  topLabel: string;
  mainTitle: string;
  description: string;
  button: string;
  imgPath: string;
};

type Currency = {
  name: string;
  symbol: string;
};

export type { Language, HomePageBanner, Currency };
